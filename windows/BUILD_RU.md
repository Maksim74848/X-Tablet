# Windows installer

Сборка требует Inno Setup 6. Положите собранный `X-Tablet.exe` в `client/dist/windows/`, после чего откройте `X-Tablet.iss` в Inno Setup и нажмите Compile.

Перед компиляцией замените `xtablet.ico` на официальный значок проекта.
